"use strict";

// Configuración de rutas para el módulo de búsqueda
// Nota: Las rutas ahora están definidas en core.client.routes.js
// para evitar conflictos de configuración
angular.module("search").config([
  "$routeProvider",
  function ($routeProvider) {
    // Rutas movidas a core.client.routes.js
  },
]);
